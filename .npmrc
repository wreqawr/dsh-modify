# Run anywhere: never prompt for a modules-directory purge. The service
# manager and CI run pnpm without a TTY, and pnpm aborts on the interactive
# purge confirmation when the workspace layout changes (e.g. a package was
# added or renamed). Non-interactive purge is the intended behavior here.
confirmModulesPurge=false
